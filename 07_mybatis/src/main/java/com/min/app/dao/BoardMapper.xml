<?xml version="1.0" encoding="UTF-8" ?> <!-- 테이블마다 매퍼 하나가 나온다 -->

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.min.app.dao.BoardMapper"> <!-- namespace = 매퍼의 이름 -->

  <select id="" resultType="com.min.app.dto.BoardDTO"> <!-- select id= 셀렉트 이름 resultType= 어디에 저장할지  rs next() 를 안쓴다-->
    <!-- <if test=""></if>
         <foreach collection=""></foreach>  등을 지원한다. -->
    
  </select>

</mapper>